import React from 'react';
// The Hero section contains the primary interactive neural network canvas.
// This component renders a very subtle global ambient noise — a handful of
// slowly drifting particles visible in every non-hero section.
export default function ParticleBackground() {
  return null;
}